<div class="container">
 <div class="row">
  <h1>配送先登録/一覧</h1>
  <%= form_with model: @address, url: addresses_path, local:true do |f|%>
   <table class="table">
    <tbody>
     <tr>
      <td>郵便番号(ハイフンなし)</td><td><%= f.text_area :address, class:"form-control w-50" %></td>
     </tr>
     <tr>
      <td>住所</td><td><%= f.text_area :postal_code, class:"form-control w-100" %></td>
     </tr>
     <tr>
      <td>宛名</td><td><%= f.text_area :name, class:"form-control w-50" %></td><td><%= f.submit "新規登録" %></td>
     </tr>
   </table>
  <% end %>
     <div class="container">
      <table class="table table-bordered">
       <tr>
        <td>郵便番号</td>
        <td>住所</td>
        <td>宛名</td>
       </tr>
       <% @addresses.each do |address|%>
       <tr>
        <td><%= address.address %></td>
        <td><%= address.postal_code %></td>
        <td><%= address.name %></td>
        <td><%= link_to "編集する", edit_address_path(address.id), class:"btn btn-success" %></td>
        <td><%= link_to "削除する", address_path(address.id), method: :delete, "date-confim"=>"本当に削除しますか?", class:"btn btn-danger" %></td>
       <% end %>
       </tr>
      </table>
     </div>
    </tbody>
  
 </div>
</div>